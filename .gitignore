# Docker persistent data
docker/data/

# WordPress uploads (content gitignored, but directory tracked)
wp-content/uploads/*
!wp-content/uploads/.gitkeep

# Environment secrets
.env
docker/.env

# OS
.DS_Store
Thumbs.db
